language: go
go:
  - 1.5
  - 1.6
  - tip

env:
  - BROKER_URI=amqp://guest:guest@localhost/

before_install:
  - go get -u github.com/golang/lint/golint

install:
  - make deps
  - make build

services:
  - rabbitmq

script:
  - make test
  - go vet
  - golint ./..

