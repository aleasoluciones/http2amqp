language: go
go:
  - 1.11.x
  - 1.17.x

sudo: false

env:
  - BROKER_URI=amqp://guest:guest@localhost/

services:
  - rabbitmq

addons:
  apt:
    packages:
    - rabbitmq-server

# safelist branch to build
branches:
  only:
    - master

script:
  - make travis

notifications:
  email:
  - bifer@alea-soluciones.com
